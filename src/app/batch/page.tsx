'use client';

import PlaceholderPage from '@/components/PlaceholderPage';
import { Users } from 'lucide-react';

export default function BatchPage() {
    return (
        <PlaceholderPage
            title="Batchmates"
            description="Connect with your batchmates, view directory, and organize reunions."
            icon={Users}
        />
    );
}
